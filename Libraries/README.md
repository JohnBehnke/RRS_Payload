These are the libraries we're using for the Adafruit sensors (Maybe other stuff, if we want)

Just copy them into your Arduino libraries folder. In Linux, /usr/share/arduino/libraries/ or something like that.
